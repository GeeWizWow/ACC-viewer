import React from 'react';

export default ({ color }) => (
    <div 
        style={{
            borderRadius: '99%',
            width: '10px',
            height: '10px',
            backgroundColor: color,
        }}
    />
);